[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python migrate.py && gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 60"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
